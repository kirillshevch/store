%div.facebook
  = link_to image_tag("facebookicon.png"), user_omniauth_authorize_path(:facebook)
= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { id: "signup", class: "forms-signin" }) do |f|
  = devise_error_messages!
  %h2.form-signin-heading or
  = f.label :email, class: "sr-only"
  %br/
  = f.email_field :email, autofocus: true, class: "forms-control", placeholder: "Your email"
  = f.label :password, class: "sr-only"
  = f.password_field :password, autocomplete: "off", class: "forms-control", placeholder: "Password"
  .actions
    = f.submit "Sign up", class: "btn btn-lg btn-primary btn-block"
  %p Have an account? #{link_to "Sign in", new_session_path(resource_name)}
